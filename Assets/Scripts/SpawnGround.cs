using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnGround : MonoBehaviour
{

    //tenes que poner un contador de tiempo y que vaya spawneando los pisos a medida que pasa el tiempo.
    //ver si el profesor enseña otro metodo
    


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
